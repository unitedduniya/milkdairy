version: '2'
services:
    milkdairy-mongodb:
        image: mongo:3.6.3
        ports:
            - "27017:27017"
        command: mongos --configdb milkdairy-mongodb-config
    milkdairy-mongodb-node:
        build:
            context: .
            dockerfile: mongodb/MongoDB.Dockerfile
        command: mongod --replSet rs1 --noprealloc --smallfiles
    milkdairy-mongodb-config:
        image: mongo:3.6.3
        container_name: milkdairy-mongodb-config
        command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
